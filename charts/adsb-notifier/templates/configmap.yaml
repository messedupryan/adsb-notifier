apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "adsb-notifier.fullname" . }}-config-seed
  labels:
    {{- include "adsb-notifier.labels" . | nindent 4 }}
data:
  {{ .Values.config.fileName }}: |
    {{- .Values.seedConfig | toPrettyJson | nindent 4 }}
